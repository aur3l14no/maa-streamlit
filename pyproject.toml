[project]
name = "maa-streamlit"
version = "0.1.0"
dependencies = [
    "streamlit",
    "psutil",
    "deepmerge",
    "loguru",
    "cattrs",
]

[project.optional-dependencies]
dev = [
    "pytest",
    "ipython",
    "ruff",
]
